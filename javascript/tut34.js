console.log("this is the asynchronous tutorial 34");

// with the help of we can achive the asynchronous progamming easily
setTimeout(() => {
for (let index = 0; index < 4000; index++) {
    const element = index;
    console.log(element);
    
}
}, 100);

console.log("this is the asynchronouse ")